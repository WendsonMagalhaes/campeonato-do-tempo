/**
 * @deprecated Telão bracket is now `copa-ui/runtime/BracketScene` (FixedCanvas layered).
 * Kept only as a compatibility re-export; do not use for new UI.
 */
export { BracketScene as BracketTree } from '../../copa-ui/runtime/BracketScene.tsx'
