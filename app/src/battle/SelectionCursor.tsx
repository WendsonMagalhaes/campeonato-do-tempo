/**
 * Legacy battle SelectionCursor stub.
 * TF / Round 3 no longer render sprite cursors (CSS slot highlight instead).
 * Component kept for optional reuse / frame tooling.
 */
export { SelectionCursor } from '../copa-ui/components/SelectionCursor.tsx'
export type { CursorAnimState as CursorState, CursorPlayer } from '../copa-ui/components/selectionCursorFrames.ts'
